<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.greencom.android.podcasts2">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- TODO: Setup backup -->
    <application
        android:name=".PodcastsApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_podcasts_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_podcasts_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PodcastsCompose">

        <!-- TODO: Make sure configChanges works correctly -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.PodcastsCompose.Starting"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>
